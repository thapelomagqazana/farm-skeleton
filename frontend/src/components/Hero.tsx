import React from "react";

const Hero: React.FC = () => {
  return (
    <div className="bg-blue-600 text-white text-center py-16">
      <h1 className="text-4xl font-bold">FARM Skeleton Starter Pack</h1>
      <p className="mt-2 text-lg">FastAPI + React + MongoDB + Tailwind CSS</p>
    </div>
  );
};

export default Hero;
